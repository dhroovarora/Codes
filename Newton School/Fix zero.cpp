https://my.newtonschool.co/playground/code/v6fen3g06rpa




#include <bits/stdc++.h> // header file includes every Standard library
using namespace std;

int main() {
	// Your code here
    int a,b,c,d,e;
    cin >> a >> b >> c >> d >> e;
    (a == 0) ? cout << 1 : (b==0) ? cout << 2 : (c==0) ? cout << 3 : (d==0) ? cout << 4 : cout << 5;
    return 0;
}